# Engineering Logbook — 2026-05-07

**Date**: 2026-05-07
**Engineer**: Yunpeng
**Activity**: M1.1 – Pi5 Hardware Component Bring-up — script creation and camera test execution

---

## Goal of the Day

Create M1.1 hardware bring-up scripts for Pi5 (camera, LEDs, buzzer, ultrasonic sensor).
Execute the camera test remotely where possible. LED, buzzer, and ultrasonic tests require
physical wiring confirmation and are marked TODO pending manual execution.

---

## Work Performed

1. Read hardware/gpio_mapping.md, hardware/wiring.md, requirements/interfaces.yaml, and
   M1 project management documents to confirm GPIO assignments and wiring requirements.
2. Checked Pi5 environment: Ubuntu 24.04.4 LTS, Python 3.12.3, gpiochip4 (RP1 southbridge),
   no GPIO libraries installed, no camera tools installed, /dev/video* devices present.
3. Created `pi5_linux/scripts/camera_test.sh`.
4. Created `pi5_linux/scripts/led_test.py`.
5. Created `pi5_linux/scripts/buzzer_test.py`.
6. Created `pi5_linux/scripts/ultrasonic_test.py`.
7. Created `pi5_linux/scripts/README.md`.
8. Executed camera_test.sh on Pi5 via SSH (see evidence below).
9. LED, buzzer, and ultrasonic tests NOT executed — GPIO packages not yet installed and
   physical wiring not yet confirmed.

---

## Environment Findings (Pi5)

| Item | Finding |
|---|---|
| OS | Ubuntu 24.04.4 LTS (Noble Numbat) |
| Kernel | 6.8.0-1053-raspi |
| Python | 3.12.3 |
| GPIO chip | /dev/gpiochip4 (RP1 southbridge, Pi5-specific) |
| GPIO libraries | None installed — requires `sudo apt install python3-lgpio` |
| Camera tools | None installed — requires `sudo apt install rpicam-apps v4l-utils` |
| /dev/video* | Present (multiple devices found) |
| Camera device tree | cam0 and cam1 entries present |

**Note**: `RPi.GPIO` is not compatible with Raspberry Pi 5. The correct library is `lgpio`
(gpiochip4). This is documented in all GPIO scripts.

---

## M1.1 – Pi5 Hardware Component Bring-up

> This is manual hardware bring-up only. No ROS2, no AI inference, no safety decisions.
> Educational demonstrator — not ISO 26262 certified.

### Camera Test (TC-M11-001 / TC-M11-002)

| Check | Command | Result | Notes |
|---|---|---|---|
| camera_test.sh executed | `bash pi5_linux/scripts/camera_test.sh` | **PASS** | Run on RPi OS 2026-05-07 |
| rpicam-hello available | auto-detected | **PASS** | `/usr/bin/rpicam-hello` — libcamera 0.7.0 |
| v4l2-ctl available | auto-detected | **PASS** | `/usr/bin/v4l2-ctl` |
| /dev/video* devices | `ls /dev/video*` | **PASS** | 25 devices: rp1-cfe (CSI), pispbe (ISP), rpi-hevc-dec |
| Camera device tree | `/proc/device-tree/cam*` | **PASS** | cam0_clk, cam0_reg, cam1_clk, cam1_reg entries present |
| Camera module detected (`rpicam-hello --list-cameras`) | `rpicam-hello --list-cameras` | **PASS** | IMX708 detected at 4608×2592 |
| Still capture | `rpicam-still -o /tmp/test_capture.jpg` | **PASS** | 625 KB JPEG at 4608×2592 |

**TC-M11-001 PASS. TC-M11-002 PASS.**

**Camera detect output:**
```
Available cameras
-----------------
0 : imx708 [4608x2592 10-bit RGGB] (/base/axi/pcie@1000120000/rp1/i2c@88000/imx708@1a)
    Modes: 'SRGGB10_CSI2P' : 1536x864 [120.13 fps]
                             2304x1296 [56.03 fps]
                             4608x2592 [14.35 fps]
```

**Still capture output:**
```
Still capture image received
-rw-rw-r-- 1 yunpeng yunpeng 625K May  6 15:34 /tmp/test_capture.jpg
```

**M1.0 re-verification on Raspberry Pi OS:**

| Check | Result | Notes |
|---|---|---|
| Pi5 OS | **PASS** | Raspberry Pi OS Debian 13 Trixie, kernel 6.12.75+rpt-rpi-2712 |
| Pi5 LAN IP | **PASS** | 192.168.0.160 (wlan0) |
| Pi5 supervisor Ethernet IP | **PASS** | 192.168.50.10/24 (eth0) — auto-configured |
| Pi5 → Pi400 ping | **PASS** | 4/4, 0% loss, avg 0.211 ms |
| Pi400 → Pi5 ping | **PASS** | 4/4, 0% loss, avg 0.204 ms |
| Repo clone on Pi5 | **PASS** | Cloned at 94cd46e |

### LED Test (TC-M11-003 / TC-M11-004 / TC-M11-005)

| Component | GPIO | Physical Pin | Result | Notes |
|---|---|---|---|---|
| Green LED (NORMAL indicator) | GPIO 17 | Pin 11 | **PASS** | Visual ON/OFF confirmed by operator |
| Yellow LED (DEGRADED indicator) | GPIO 27 | Pin 13 | **PASS** | Visual ON/OFF confirmed by operator |
| Red LED (SAFE STATE indicator) | GPIO 22 | Pin 15 | **PASS** | Visual ON/OFF confirmed by operator |

**TC-M11-003 PASS. TC-M11-004 PASS. TC-M11-005 PASS.**

**Wiring (confirmed)**:
- GPIO 17 (Pin 11) → Green LED → GND (NORMAL state indicator)
- GPIO 27 (Pin 13) → Yellow LED → GND (DEGRADED state indicator)
- GPIO 22 (Pin 15) → Red LED → GND (SAFE STATE indicator)

**Wiring deviation note**: GPIO 17 and GPIO 27 were originally assigned to heartbeat output
and emergency stop input in `hardware/gpio_mapping.md`. They are used for LEDs in M1.1
bring-up. Heartbeat and e-stop GPIO assignments will be moved to unallocated pins before
M2 integration. `gpio_mapping.md` updated to reflect this.

### Buzzer Test (TC-M11-006)

| Check | Result | Notes |
|---|---|---|
| Audible tone heard | TODO | Wiring not yet confirmed |

**Wiring plan** (from buzzer_test.py):
- GPIO 26 (Pin 37) → Buzzer (+), GND (Pin 39) → Buzzer (−)
- Active 3.3 V buzzer required (not passive)

**Note**: GPIO 26 not yet in hardware/gpio_mapping.md. Confirm and update during wiring.

### Ultrasonic Sensor Test (TC-M11-007)

| Distance | Expected (cm) | Measured (cm) | Result | Notes |
|---|---|---|---|---|
| ~20 cm | 20 ± 2 | — | TODO | Wiring and voltage divider not yet confirmed |
| ~50 cm | 50 ± 5 | — | TODO | Wiring and voltage divider not yet confirmed |

**Prerequisite**: HC-SR04 ECHO voltage divider (1 kΩ / 2 kΩ) MUST be built before
connecting ECHO to GPIO 24 (Pin 18). Skipping this risks GPIO damage (5 V on 3.3 V pin).

### Wiring Deviation Review (TC-M11-008)

| Item | Status | Notes |
|---|---|---|
| gpio_mapping.md — LED GPIOs | TODO | Pins 16/20/21 used in scripts; not yet in mapping doc |
| gpio_mapping.md — Buzzer GPIO | TODO | GPIO 26 used in script; not yet in mapping doc |
| ECHO voltage divider built | TODO | Required before running ultrasonic_test.py |
| Physical wiring matches docs | TODO | Pending hardware assembly |

---

## Platform Decision — Pi5 OS Change

**Decision (2026-05-07)**: Switch Pi5 from Ubuntu 24.04 LTS to Raspberry Pi OS (Debian 13 Trixie).

**Reason**: Ubuntu 24.04 ships libcamera 0.2.0 which cannot support Raspberry Pi cameras
with the current Raspberry Pi package set. The Raspberry Pi libcamera 0.5.x packages have
hard dependency conflicts with Ubuntu 24.04 (require Python < 3.12, `libjpeg62-turbo`,
`libavcodec59` — all Debian Bookworm/Trixie-specific). Raspberry Pi OS has full camera
support out of the box via `rpicam-apps` and libcamera 0.7.0.

**Additional finding**: Camera module is **IMX708** (Raspberry Pi Camera Module 3), not
IMX500. IMX500 firmware installation was unnecessary. IMX708 is detected and fully functional
on Raspberry Pi OS without any extra packages.

**Pi5 OS after flash**: Raspberry Pi OS (Debian GNU/Linux 13 Trixie), kernel 6.12.75+rpt-rpi-2712,
libcamera 0.7.0+rpt20260205, libpisp 1.4.0.

**Impact**: M1.0 platform access re-verified — see section below. Pi400 remains on Ubuntu
and is unaffected.

## Open Items

| ID | Item | Owner | Target |
|----|------|-------|--------|
| OP-M11-000 | ~~Flash Raspberry Pi OS to Pi5~~ | ~~Yunpeng~~ | **Done 2026-05-07** |
| OP-M11-001 | ~~Camera test on RPi OS~~ | ~~Yunpeng~~ | **Done 2026-05-07** — IMX708 PASS |
| OP-M11-002 | ~~Install python3-lgpio on Pi5~~ | ~~Yunpeng~~ | **Done 2026-05-07** |
| OP-M11-003 | ~~Wire and test LEDs~~ | ~~Yunpeng~~ | **Done 2026-05-07** — GPIO 17/27/22, all PASS |
| OP-M11-004 | Wire buzzer to GPIO 26; run buzzer_test.py | Yunpeng | After OS flash |
| OP-M11-005 | Build ECHO voltage divider (1 kΩ / 2 kΩ); wire HC-SR04; run ultrasonic_test.py | Yunpeng | After OS flash |
| OP-M11-006 | Update hardware/gpio_mapping.md with confirmed LED and buzzer GPIO assignments | Yunpeng | After wiring confirmed |
| OP-001 | Pi400 GPIO pin assignments for heartbeat/e-stop (TBD) | Yunpeng | M2/M5 |

---

## Next Steps

1. On Pi5: `sudo apt install rpicam-apps v4l-utils python3-lgpio`
2. Run `bash pi5_linux/scripts/camera_test.sh` and paste output here.
3. Wire LEDs, buzzer, and ultrasonic sensor per README wiring tables.
4. Confirm voltage divider on HC-SR04 ECHO with a multimeter before connecting GPIO.
5. Run `led_test.py`, `buzzer_test.py`, `ultrasonic_test.py` in that order.
6. Record all results in this logbook; update evidence table rows from TODO → PASS/FAIL.
7. Update `hardware/gpio_mapping.md` with confirmed LED and buzzer pin assignments.

---

*Engineer: Yunpeng*
*Logbook entry: session complete — physical wiring and package installation pending*

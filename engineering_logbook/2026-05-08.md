# Engineering Logbook — 2026-05-08

**Date**: 2026-05-08
**Engineer**: Yunpeng
**Activity**: M2 start — ROS2 Humble environment setup on Pi5 (Raspberry Pi OS Bookworm)

---

## Goal of the Day

Establish a working ROS2 Humble environment on Pi5 as the foundation for M2 sensor pipeline
implementation (health_node, ultrasonic_node, camera_ai_node).

---

## ROS2 Journey Summary

**Goal**: Install ROS2 on Pi5 to support M2 sensor pipeline development.

**What happened — four attempts before success:**

1. **Raspberry Pi OS Trixie (Debian 13) — no apt packages.**
   The packages.ros.org repository has no ROS2 entries for Bookworm/Trixie when using the
   standard Ubuntu-oriented setup. `apt-cache search ros-jazzy` returned nothing.

2. **Docker on Trixie considered — rejected.**
   Docker would run a Ubuntu 24.04 container natively on Pi5 ARM64 (no emulation, full MMU
   support). However, libcamera inside a Ubuntu 24.04 container cannot access the Raspberry Pi
   IPA modules from the RPi OS Trixie host kernel — the same camera incompatibility we already
   solved by leaving Ubuntu. Docker would have reintroduced it.

3. **Raspberry Pi OS Bookworm (Debian 12) — same apt problem.**
   Reflashed to Bookworm (Python 3.11, better ROS2 compatibility). Added packages.ros.org
   repository with "bookworm" codename. `apt update` succeeded but `apt-cache search ros-jazzy`
   and `ros-humble` returned only ROS1 packages from standard Debian repos. No ROS2 via apt.

4. **Ubuntu 22.04 considered — rejected on camera grounds.**
   Would have official ROS2 Humble apt packages. But Ubuntu ships upstream libcamera which
   lacks the Raspberry Pi IPA modules (`ipa_rpi_pisp.so`) needed for the IMX708. Same camera
   failure as Ubuntu 24.04. No Ubuntu version solves both camera and ROS2 simultaneously.

**Root cause of the conflict:**
- ROS2 via apt requires Ubuntu (22.04 for Humble, 24.04 for Jazzy)
- RPi Camera Module 3 (IMX708) requires Raspberry Pi OS (which ships RPi's libcamera fork)
- These are mutually exclusive via package manager

**Solution: Build ROS2 Humble from source on Raspberry Pi OS Bookworm.**
- Bookworm + Python 3.11 is a supported combination for Humble
- Camera works natively (rpicam-apps, libcamera 0.3.x with RPi IPA modules)
- ROS2 built from source with colcon — 287 packages, ~49 minutes on Pi5
- `ros2` CLI and `rclpy` verified working

---

## ROS2 Installation — Build from Source on Bookworm

### Decision rationale

| Option | Outcome |
|---|---|
| ROS2 Jazzy via apt on Bookworm | No packages available for Bookworm |
| Docker + ROS2 Jazzy on Trixie | Reintroduces libcamera mismatch inside container |
| Ubuntu 22.04 | Same libcamera incompatibility as 24.04 (upstream libcamera, no RPi IPA) |
| **Build ROS2 Humble from source on Bookworm** | **Camera works natively; Python 3.11 supported by Humble** |

### Environment

| Item | Value |
|---|---|
| OS | Raspberry Pi OS Bookworm (Debian 12) |
| Kernel | 6.12.75+rpt-rpi-2712 |
| Python | 3.11.2 |
| ROS2 | Humble (built from source) |
| Build time | ~49 min total (287 packages across two colcon runs) |
| Install path | `~/ros2_humble/install/` |

### Build steps executed

| Stage | Command | Result |
|---|---|---|
| 1 | `sudo apt install` build dependencies | PASS |
| 2 | `sudo rosdep init && rosdep update` | PASS (WARNING: ROS_PYTHON_VERSION unset — benign) |
| 3 | `vcs import src < ros2.repos` | PASS — sources cloned |
| 4 | `rosdep install --from-paths src` | PASS — all rosdeps installed |
| 5 | `colcon build` — core packages | PASS — 129 packages, 45 min 19 s |
| 6 | `colcon build` — ros2cli packages | PASS — 158 packages, 3 min 52 s |

### Verification

| Check | Result |
|---|---|
| `ros2` CLI available | PASS — `ros2 --help` works |
| `import rclpy` | PASS — `rclpy OK` |

### Workspace source

Add to every shell session (already in `~/.bashrc`):
```bash
source ~/ros2_humble/install/setup.bash
```

---

## M1.1 Re-verification — Bookworm + ROS2 Humble Environment

Re-ran all M1.1 hardware bring-up tests and Ethernet smoke test to confirm hardware
still functional after OS stabilisation (Bookworm) and ROS2 Humble source build.

| Test | Script | Result | Notes |
|---|---|---|---|
| LED — Green (NORMAL) | `led_test.py` | **PASS** | Visual ON/OFF confirmed by operator |
| LED — Yellow (DEGRADED) | `led_test.py` | **PASS** | Visual ON/OFF confirmed by operator |
| LED — Red (SAFE STATE) | `led_test.py` | **PASS** | Visual ON/OFF confirmed by operator |
| Camera detection | `camera_test.sh` | **PASS** | All 5 steps pass; IMX708 detected |
| Ultrasonic sensor | `ultrasonic_test.py` | **PASS** | Valid readings confirmed by operator |
| Buzzer | `buzzer_test.py` | **PASS** | Audible tone confirmed by operator |
| Ethernet smoke test (Pi5 → Pi400 UDP) | `udp_sender.py` / `udp_receiver.py` | **PASS** | UDP packets received on Pi400 at 192.168.50.20:5005 |

All M1.1 hardware confirmed functional on Bookworm + ROS2 Humble. Hardware baseline
is stable for M2 sensor pipeline implementation.

---

## Open Items

| ID | Item | Owner | Target |
|----|------|-------|--------|
| OP-M20-001 | Implement health_node (heartbeat GPIO generation) | Yunpeng | M2 |
| OP-M20-002 | Implement ultrasonic_node (Grove Ultrasonic Ranger via lgpio) | Yunpeng | M2 |
| OP-M20-003 | Implement camera_ai_node (IMX708 capture via picamera2/libcamera) | Yunpeng | M2 |
| OP-M20-004 | Reassign GPIO 17/27 from LEDs to heartbeat/e-stop before node implementation | Yunpeng | M2 |
| OP-M20-005 | Add vision_msgs and picamera2 to ROS2 workspace if needed | Yunpeng | M2 |

---

## Next Steps

1. Create ROS2 workspace structure under `pi5_linux/ros2_ws/`.
2. Implement `health_node` first — simplest node, validates ROS2 + GPIO integration.
3. Implement `ultrasonic_node` — validates lgpio + ROS2 topic pipeline.
4. Implement `camera_ai_node` — validates camera capture in ROS2 context.
5. Write unit tests for deterministic logic per AGENTS.md requirements.
